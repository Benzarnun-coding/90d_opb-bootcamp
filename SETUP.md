# Creator Bootcamp — คู่มือติดตั้งและดูแลรุ่น

เว็บแข่งปล่อยคอนเทนต์สำหรับ bootcamp 12 สัปดาห์
นักเรียนล็อกอินด้วยอีเมลที่คุณอัปโหลดไว้ เลือกเองว่าแต่ละสัปดาห์จะปล่อยกี่ชิ้น
ส่งลิงก์แล้วตัวละครวิ่งทันที ทุกคนเห็นสนามแข่งเดียวกันแบบเรียลไทม์

---

## กติกาของเกม

| เรื่อง | กติกา |
|---|---|
| ความยาว | **90 วัน** — 6 สปรินต์ สปรินต์ละ 2 สัปดาห์ (84 วัน) แล้วต่อเวลาอีก 6 วัน |
| เป้ารวม | **90 คอนเทนต์** ส่งเกินได้ ไม่มีเพดาน |
| ช่วงต่อเวลา | วันที่ 85–90 ส่งงานได้ปกติ แต่ไม่มีเป้ารายสัปดาห์ เป็นช่วงเก็บตกให้ถึง 90 ชิ้น |
| เป้ารายสัปดาห์ | เลือกเองทุกสัปดาห์ — 4 / 7 / 10 ชิ้น |
| ตัวเลือกลับ | 14 ชิ้น PRO MAX **ซ่อนไว้ไม่ให้เห็นเลย** จนถึงสัปดาห์ที่ 7 แล้วโผล่มาเอง |
| บทลงโทษ | รับ 10 หรือ 14 แล้วทำไม่ถึง สัปดาห์ถัดไปกลายเป็น **ร่างกระโหลก** เลือกได้แค่ 4 หรือ 7 |
| การนับ | ส่งลิงก์แล้ว**นับทันที** ไม่มีระบบตรวจงาน วันหนึ่งส่งกี่ชิ้นก็ได้ |
| ห้อง | 4 ห้อง — WISDOM 🦉 / JUSTICE ⚖️ / COURAGE 🦁 / DISCIPLINE 🛡️ |
| ตัดรอบวัน | ตี 4 ตามเวลาไทย (โพสต์ตีหนึ่งยังนับเป็นของเมื่อวาน) |

**ตัวละครเปลี่ยนตามเป้าที่รับ**

| เป้า | ตัวละคร |
|---|---|
| 4 / 7 | สีที่ตัวเองเลือก ปกติ |
| 10 LASER FOCUS | ชุดแดงทั้งตัว **ตายิงเลเซอร์** |
| 14 PRO MAX | ชุดทอง ผมทอง **ตาแดง** มีไฟลุกรอบตัว |
| 💀 หมดแรง | **ร่างกระโหลกสีกระดูก** ขยับโซเซ ไม่มีแสง |

**บทลงโทษเมื่อรับปากเกินตัว** ใครรับ 10 หรือ 14 ไว้แล้วสัปดาห์นั้นทำไม่ถึง สัปดาห์ถัดไป
จะกลายเป็นร่างกระโหลกทั้งสัปดาห์ และเลือกได้แค่ 4 หรือ 7 เท่านั้น พอทำสัปดาห์นั้นครบ
สัปดาห์ถัดไปก็กลับมาเลือกเป้าหนักได้เหมือนเดิม — บังคับที่ฐานข้อมูล ไม่ใช่แค่ซ่อนปุ่ม

**เลือก 7 ทุกสัปดาห์แล้วทำครบเป๊ะจะได้ 84 ชิ้น ขาดอีก 6** ซึ่งพอดีกับช่วงต่อเวลา 6 วันท้าย
ใครเดินสาย RECOMMENDED อย่างมีวินัยจะถึง 90 พอดีในวันสุดท้าย ส่วนคนที่รับ LASER FOCUS
ระหว่างทางจะถึงก่อนแล้ววิ่งเลยเส้นไปได้

---

## ไฟล์ในโปรเจกต์

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | หน้าเว็บทั้งหมด โครงสร้าง + ดีไซน์ |
| `app.js` | ตรรกะทั้งหมด — คะแนน สนามแข่ง การ์ดแชร์ เชื่อมฐานข้อมูล |
| `config.js` | **ไฟล์เดียวที่ต้องแก้** — คีย์ Supabase, เป้า, สปรินต์, ตัวเลือกรายสัปดาห์ |
| `backend/` | ฐานข้อมูล — migration, ข้อมูลทดสอบ, ชุดเทสต์ ดูแผนงานใน `backend/README.md` |
| `netlify.toml` | ตั้งค่า deploy + security headers |
| `serve.js` | เซิร์ฟเวอร์เล็ก ๆ ไว้เปิดดูในเครื่อง |

---

## โหมด DEMO — ดูก่อนได้เลย ไม่ต้องตั้งค่าอะไร

```bash
node serve.js
```

เปิด `http://localhost:5173` จะได้ข้อมูลปลอม 13 คนไว้กดเล่นดูหน้าตาระบบ
ปุ่ม `SIMULATE 1 DAY` หมุนเวลาไปข้างหน้า กดรัว ๆ จนถึงสัปดาห์ 7 จะเห็น PRO MAX ปลดล็อก

> เปิด `index.html` ด้วยการดับเบิลคลิกไม่ได้ เบราว์เซอร์จะไม่ยอมโหลด `config.js`
> ต้องเปิดผ่าน `node serve.js` เท่านั้น

โหมดนี้เก็บข้อมูลในเบราว์เซอร์เครื่องเดียว **ห้ามเอาไปใช้กับนักเรียนจริง**

---

## ทำให้ใช้งานจริง

### 1. สร้างโปรเจกต์ Supabase

[supabase.com](https://supabase.com) → **New project** → region `Southeast Asia (Singapore)`
สร้าง **สองอัน** คือ `bootcamp-dev` ไว้ทดลอง กับ `bootcamp-prod` ไว้ใช้จริง
อย่าทดลองบนตัวจริงเพราะไฟล์ seed มีคำสั่งลบข้อมูลทิ้ง

รุ่น 250 คนใช้พื้นที่ไม่ถึง 30MB จากลิมิต 500MB ของแพ็กเกจฟรี เรื่องพื้นที่ไม่ต้องห่วง
แต่ **realtime ของแพ็กเกจฟรีรับได้ 200 การเชื่อมต่อพร้อมกัน** ซึ่งน้อยกว่าจำนวนนักเรียน
ถ้าไม่อยากอัปเป็น Pro (~$25/เดือน) ให้ปิด realtime แล้วดึงข้อมูลใหม่ทุก 30 วินาทีแทน
ซึ่งสำหรับเกมที่เดินเป็นวัน ๆ แทบไม่ต่างกันเลย

### 2. สร้างตาราง

**SQL Editor** → วางทีละไฟล์แล้วกด RUN ตามลำดับ

```
backend/migrations/001_core.sql          ตาราง + RLS + trigger
backend/migrations/002_scale.sql         view สรุปผล + index
backend/migrations/003_houses.sql        4 ห้อง + รายชื่ออีเมล
backend/migrations/004_weekly_pledge.sql คำสัญญารายสัปดาห์
backend/migrations/005_no_review.sql     ส่งแล้วนับทันที + เป้า 90
backend/migrations/006_notify.sql        คิวแจ้งเตือน
backend/migrations/007_start_date.sql    วันเปิดรุ่น + กันส่งงานก่อนเริ่ม
backend/migrations/008_overtime.sql      ต่อเวลา 6 วันให้ครบ 90
```

จากนั้นตั้งวันเริ่มรุ่น — **วันที่นี้สำคัญที่สุดในไฟล์ ตั้งผิดทั้งรุ่นเลื่อนหมด**

```sql
update public.cohort set start_date = '2026-09-01', goal_total = 90 where id = 1;
```

### 3. ตรวจว่ากติกาบังคับได้จริง

รัน `backend/test/smoke.sql` ต้องขึ้น **PASS ครบทั้ง 13 ข้อ** ถ้ามี FAIL อย่าเพิ่งเปิดใช้
ชุดนี้ทดสอบว่ายัดสถานะเองไม่ได้ ส่งลิงก์ซ้ำไม่ได้ ลดเป้ากลางสัปดาห์ไม่ได้
เลือกเป้าย้อนหลังไม่ได้ และนักเรียนตั้งตัวเองเป็นโค้ชไม่ได้

ถ้าอยากลองว่ารับ 250 คนไหว รัน `backend/seed/dev_seed_250.sql` (**บน dev เท่านั้น**)
แล้วดู `explain analyze select * from v_leaderboard` ควรต่ำกว่า 300ms

### 4. เอาคีย์มาใส่ config.js

**Project Settings → Data API** คัดลอกสองค่านี้

```js
SUPABASE_URL: "https://xxxxxxxx.supabase.co",
SUPABASE_ANON_KEY: "eyJhbGciOi...",
```

`anon key` เปิดเผยได้ ปลอดภัย เพราะฐานข้อมูลมี RLS คุมอยู่
**ห้ามเอา `service_role` key มาใส่เด็ดขาด** อันนั้นข้ามการป้องกันทั้งหมด

### 5. ตั้งค่าการล็อกอิน

**Authentication → URL Configuration**
Site URL ใส่ URL จริงหลัง deploy, Redirect URLs ใส่ URL เดียวกันบวก `http://localhost:5173`

**ที่ 250 คนต้องใช้ Google login ไม่มีทางเลือกอื่น** อีเมลในตัวของ Supabase ส่งได้แค่ไม่กี่ฉบับ
ต่อชั่วโมง วันเปิดรุ่นจะมีคนเข้าได้ 3–4 คนแล้วที่เหลือค้าง
ไปที่ **Authentication → Sign In / Providers → Google** ทำตามขั้นตอนที่หน้านั้นบอก
แล้วตั้ง `GOOGLE_AUTH: true` ใน `config.js`

### 6. เอาเว็บขึ้นออนไลน์

ไฟล์นิ่งล้วน มี `netlify.toml` เตรียมไว้แล้ว
ลากทั้งโฟลเดอร์ไปวางที่ [app.netlify.com/drop](https://app.netlify.com/drop) ได้ URL ใน 30 วินาที
หรือ push ขึ้น GitHub แล้วต่อกับ Netlify / Vercel / Cloudflare Pages

**ต้องเป็น https** เพราะปุ่มแชร์การ์ดใช้ `navigator.share` ซึ่งไม่ทำงานบน http
เสร็จแล้วกลับไปแก้ Site URL ในข้อ 5 ให้ตรงกับ URL จริง

### 7. ตั้งตัวเองเป็นหัวหน้าโค้ช

เข้าเว็บ สมัครด้วยอีเมลตัวเองหนึ่งครั้ง แล้วรัน

```sql
update public.profiles set role = 'coach', house_id = null where name = 'BENZ';
```

`house_id = null` คือหัวหน้าโค้ช เห็นทุกห้องและจัดการรายชื่อได้
ถ้าจะตั้งผู้ช่วยประจำห้อง ใส่เลขห้องแทน เช่น `house_id = 3` จะเห็นเฉพาะห้อง COURAGE

### 8. อัปโหลดรายชื่อนักเรียน

**ทำจากหน้าเว็บได้เลย** พอเป็นหัวหน้าโค้ชจะเห็นเมนู **⚙️ ADMIN**
วางอีเมลทีละหลายบรรทัด เลือกห้อง กดปุ่มเดียว ใส่ชื่อจริงต่อท้ายหลังจุลภาคก็ได้

```
somchai@gmail.com, สมชาย
malee@gmail.com, มาลี
nattapong@gmail.com
```

**ใครไม่มีชื่อในรายชื่อจะสมัครไม่ได้เลย** รายชื่อนี้ทำหน้าที่เป็นประตูของทั้งระบบ
ใครไม่ได้จ่ายค่าคอร์สก็เข้าไม่ได้ และหนึ่งอีเมลสมัครได้ครั้งเดียว

> เตรียมคนไว้ตอบวันแรก เพราะจะมีคนสะกดอีเมลตอนสมัครคอร์สไม่ตรงกับที่ใช้ล็อกอินจริง
> เขาจะเข้าไม่ได้แล้วทักมา แก้ด้วยการเพิ่มอีเมลที่ถูกเข้าไปในหน้า ADMIN

---

## กติกาที่ฐานข้อมูลบังคับ แก้ในหน้าเว็บไม่ได้

- **ลิงก์ซ้ำส่งไม่ได้ทั้งรุ่น** ไม่ว่าใครส่ง เอาลิงก์เก่ามาส่งใหม่ก็ไม่ได้
- **ลิงก์ที่ไม่มี handle ของเจ้าตัว** จะติดธงไว้ให้ดู แต่ไม่บล็อก
- **วันที่เท่าไหร่ ระบบคิดเอง** จากเวลาเซิร์ฟเวอร์ ไม่เชื่อนาฬิกาในเครื่องนักเรียน
- **ส่งงานในสปรินต์ที่ไม่ได้ลงไม่ได้**
- **เป้าสัปดาห์นี้เพิ่มได้ ลดไม่ได้** และเลือกย้อนหลังไม่ได้
- **PRO MAX 14 ชิ้น เปิดสัปดาห์ที่ 7 เป็นต้นไป**
- **สปรินต์ที่เริ่มไปแล้วเปลี่ยนไม่ได้**
- **ลบงานไม่ได้** มีแต่หัวหน้าโค้ชที่ตั้ง `status = 'rejected'` ได้ ประวัติเก็บไว้หมด
- **นักเรียนตั้งตัวเองเป็นโค้ชไม่ได้ และย้ายห้องเองไม่ได้**

---

## ปรับให้เข้ากับหลักสูตร

แก้ `config.js` แล้ว**แก้ในฐานข้อมูลให้ตรงกันด้วย** ไม่งั้นระบบจะเพี้ยนแบบเงียบ ๆ

```js
GOAL_TOTAL: 90,         // เป้ารวม
SPRINT_DAYS: 14,        // สปรินต์ละกี่วัน
CUTOFF_HOUR: 4,         // ตัดรอบกี่โมง
SPRINTS: [ ... ],       // ชื่อกับโจทย์ของแต่ละสปรินต์
PLEDGES: [ ... ]        // ตัวเลือกรายสัปดาห์ + สัปดาห์ที่ปลดล็อก
```

```sql
update public.cohort set goal_total = 90, sprint_days = 14, sprints = 6, weeks = 12 where id = 1;
update public.pledge_options set unlock_week = 7 where target = 14;
```

ชื่อห้องกับสีแก้ได้ทั้งในตาราง `houses` และในตัวแปร `HOUSES` ที่หัวไฟล์ `app.js`

---

## คำสั่งที่ใช้ตอนดูแลรุ่น

หน้า **⚙️ ADMIN** ครอบคลุมงานประจำวันแล้ว — ใครยังไม่สมัคร ใครตามหลังเป้า
ที่เหลือคือคำสั่งที่ต้องใช้ SQL

```sql
-- ใครยังไม่เลือกเป้าของสัปดาห์นี้
select p.name, h.name as house
from public.profiles p
left join public.houses h on h.id = p.house_id
where p.role = 'student'
  and not exists (select 1 from public.pledges pl
                  where pl.profile_id = p.id and pl.week_no = public.current_week())
order by h.name, p.name;

-- สัปดาห์นี้ใครเลือกอะไรบ้าง
select o.name, count(*) from public.pledges pl
join public.pledge_options o on o.target = pl.target
where pl.week_no = public.current_week() group by o.name order by count(*) desc;

-- ภาพรวม 4 ห้อง
select * from public.v_house_board;

-- สรุปผลรายคน เอาไปทำใบประกาศตอนจบรุ่น
select name, contents, target_total, rate, week_streak, house_name
from public.v_leaderboard where role = 'student' order by contents desc;

-- ย้ายห้องให้นักเรียน (หัวหน้าโค้ชเท่านั้น)
update public.roster   set house_id = 2 where email = 'somchai@gmail.com';
update public.profiles set house_id = 2 where name  = 'SOMCHAI';

-- ลบงานที่มั่วออก
update public.submissions set status = 'rejected' where id = 12345;
```

---

## สิ่งที่ยังไม่มี

- **ตรวจว่าลิงก์มีอยู่จริงไหม** ตอนนี้เชื่อใจนักเรียน มีแค่ธงเตือนเมื่อ handle ไม่ตรง
- **แข่งเป็นทีม** ตอนนี้ห้องเป็นแค่การจัดกลุ่ม คะแนนยังเป็นรายคนล้วน

---

## เรื่องที่ไม่ใช่เทคนิคแต่ลืมไม่ได้

**บอกกติกาก่อนเริ่ม** โดยเฉพาะเรื่องเป้า 90 ชิ้น และการที่รับเป้าสัปดาห์แล้วลดไม่ได้
กติกาพวกนี้บังคับที่ฐานข้อมูล นักเรียนจะเจอ error ถ้าไม่รู้มาก่อน

**ความเป็นส่วนตัว** ทุกคนในรุ่นเห็นลิงก์งาน ชื่อ handle และสถิติของกันและกัน ต้องบอกก่อนสมัคร

**สำรองข้อมูล** export ตาราง `submissions` ทุกสัปดาห์ เพราะเป็นผลงานของคน 250 คนตลอด 3 เดือน
