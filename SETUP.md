# Creator Bootcamp — คู่มือติดตั้ง

เว็บแข่งปล่อยคอนเทนต์สำหรับ bootcamp นักเรียนสมัครด้วยอีเมลตัวเอง เลือกลงสปรินต์ที่อยากวิ่ง
ส่งลิงก์งานที่ปล่อยในแต่ละวัน โค้ชตรวจแล้วให้ดาว ตัวละครถึงจะวิ่งไปข้างหน้า
ทุกคนเห็นสนามแข่งเดียวกันแบบเรียลไทม์

---

## ไฟล์ในโปรเจกต์

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | หน้าเว็บทั้งหมด (โครงสร้าง + ดีไซน์) |
| `app.js` | ตรรกะทั้งหมด — คะแนน, สนามแข่ง, คิวตรวจงาน, เชื่อมฐานข้อมูล |
| `config.js` | **ไฟล์เดียวที่ต้องแก้** — คีย์ Supabase, ชื่อสปรินต์, กติกา |
| `backend/` | ฐานข้อมูล — migration, ข้อมูลทดสอบ, เทสต์ ดูแผนงานใน `backend/README.md` |
| `serve.js` | เซิร์ฟเวอร์เล็ก ๆ ไว้เปิดดูในเครื่อง |

---

## โหมด DEMO — ดูก่อนได้เลย ไม่ต้องตั้งค่าอะไร

```bash
node serve.js
```

เปิด `http://localhost:5173` จะได้ข้อมูลปลอม 8 คนไว้กดเล่นดูหน้าตาระบบ
มีปุ่ม `SIMULATE 1 DAY` ให้หมุนเวลาดูว่าเวลาคนหลุด streak หน้าตาเป็นยังไง

> เปิดไฟล์ `index.html` ด้วยการดับเบิลคลิกไม่ได้ เบราว์เซอร์จะไม่ยอมโหลด `config.js`
> ต้องเปิดผ่าน `node serve.js` เท่านั้น

โหมดนี้เก็บข้อมูลในเบราว์เซอร์เครื่องเดียว **ห้ามเอาไปใช้กับนักเรียนจริง** เพราะแต่ละคนจะเห็นคนละโลก

---

## ทำให้ใช้งานจริง — 6 ขั้นตอน

### 1. สร้างโปรเจกต์ Supabase

ไปที่ [supabase.com](https://supabase.com) สมัครบัญชี (ฟรี) แล้วกด **New project**

- ตั้งชื่อโปรเจกต์อะไรก็ได้
- **Region** เลือก `Southeast Asia (Singapore)` — ใกล้ไทยที่สุด เว็บจะเร็วกว่า
- ตั้ง Database password แล้วเก็บไว้ (ไม่ได้ใช้ในเว็บนี้ แต่ใช้ตอนเข้าฐานข้อมูลตรง ๆ)

รอสัก 2 นาทีให้สร้างเสร็จ

รุ่นขนาด 250 คนใช้พื้นที่ฐานข้อมูลไม่ถึง 20MB จากลิมิต 500MB ของแพ็กเกจฟรี เรื่องพื้นที่ไม่ต้องห่วง
แต่ realtime ของแพ็กเกจฟรีรับได้ 200 การเชื่อมต่อพร้อมกัน ถ้าคนเปิดเว็บพร้อมกันเกินนั้นจะมีคนหลุด
ทางเลือกคืออัปเป็น Pro (~$25/เดือน) หรือปิด realtime แล้วให้ดึงข้อมูลใหม่ทุก 30 วินาทีแทน
ข้อจำกัดอื่นของรุ่นขนาดนี้อยู่ใน `backend/README.md`

### 2. สร้างตาราง

ในหน้า Supabase เลือกเมนู **SQL Editor** ทางซ้าย → **New query**
เปิดไฟล์ `backend/migrations/001_core.sql` คัดลอกทั้งไฟล์ วางลงไป กด **RUN** แล้วทำซ้ำกับ `backend/migrations/002_scale.sql`

ถ้าขึ้น `Success. No rows returned` แปลว่าเรียบร้อย

จากนั้นตั้ง **วันเริ่มรุ่น** — วันนี้จะนับเป็นวันที่ 1 ของ bootcamp:

```sql
update public.cohort set start_date = '2026-09-01' where id = 1;
```

> วันที่นี้สำคัญมาก ระบบคำนวณ "วันนี้เป็นวันที่เท่าไหร่ของ bootcamp" จากตรงนี้
> ตั้งผิด = ทั้งรุ่นเลื่อนหมด ตั้งให้ถูกก่อนเปิดใช้

### 3. เอาคีย์มาใส่ใน config.js

Supabase → **Project Settings** → **Data API**
คัดลอก 2 ค่านี้มาวางใน `config.js`

```js
SUPABASE_URL: "https://xxxxxxxx.supabase.co",
SUPABASE_ANON_KEY: "eyJhbGciOi...",
```

`anon key` เปิดเผยได้ ปลอดภัย เพราะฐานข้อมูลป้องกันด้วย Row Level Security อยู่แล้ว
**ห้ามเอา `service_role` key มาใส่เด็ดขาด** — อันนั้นข้ามการป้องกันทั้งหมด

### 4. ตั้งค่าการล็อกอิน

Supabase → **Authentication** → **URL Configuration**

- **Site URL** ใส่ URL จริงของเว็บหลัง deploy เช่น `https://bootcamp.yourdomain.com`
- **Redirect URLs** ใส่ URL เดียวกัน และเพิ่ม `http://localhost:5173` ไว้เทสต์

#### ⚠ เรื่องที่พลาดกันบ่อยที่สุด: ลิมิตการส่งอีเมล

อีเมลในตัวของ Supabase ส่งได้แค่ **ไม่กี่ฉบับต่อชั่วโมง** ถ้านักเรียน 40 คนสมัครพร้อมกันวันเปิดรุ่น
จะมีคนได้ลิงก์แค่ 3–4 คน ที่เหลือเข้าไม่ได้เลย **ต้องแก้ก่อนเปิดรุ่น** เลือกทางใดทางหนึ่ง:

**ทางที่ 1 — ใช้ Google login (ง่ายกว่า แนะนำ)**
Supabase → Authentication → Sign In / Providers → เปิด **Google** แล้วทำตามขั้นตอนที่หน้านั้นบอก
เสร็จแล้วเปิดปุ่มในเว็บด้วยการตั้ง `GOOGLE_AUTH: true` ใน `config.js`
ไม่ต้องส่งอีเมลเลย ไม่มีลิมิต

**ทางที่ 2 — ต่อ SMTP ของตัวเอง**
สมัคร [Resend](https://resend.com) (ฟรี 3,000 ฉบับ/เดือน) แล้วเอาค่า SMTP ไปใส่ที่
Supabase → Project Settings → Authentication → SMTP Settings

### 5. เอาเว็บขึ้นออนไลน์

ไฟล์ทั้งหมดเป็นไฟล์นิ่ง ไม่ต้องมีเซิร์ฟเวอร์ วิธีที่เร็วที่สุด:

**Netlify Drop** — เปิด [app.netlify.com/drop](https://app.netlify.com/drop)
ลากโฟลเดอร์ทั้งโฟลเดอร์ไปวาง ได้ URL ทันทีภายใน 30 วินาที

หรือถ้าใช้ Git อยู่แล้วก็ push ขึ้น GitHub แล้วต่อกับ Vercel / Cloudflare Pages / GitHub Pages ได้หมด

**อย่าลืม** กลับไปแก้ Site URL ในขั้นตอนที่ 4 ให้ตรงกับ URL จริงที่ได้มา

### 6. ตั้งตัวเองเป็นโค้ช

เข้าเว็บ สมัครด้วยอีเมลตัวเองก่อน 1 ครั้ง แล้วกลับไปที่ SQL Editor:

```sql
update public.profiles set role = 'coach' where name = 'BENZ';
```

พอเป็นโค้ชแล้วจะเห็นปุ่ม **COACH** พร้อมตัวเลขงานที่ค้างตรวจ

---

## กติกาที่ระบบบังคับให้ (แก้ในเว็บไม่ได้ ป้องกันที่ฐานข้อมูล)

- **ส่งงานแล้วยังไม่นับ** ต้องให้โค้ชตรวจก่อน ระยะทางถึงจะเดิน
- **ลิงก์ซ้ำส่งไม่ได้** ทั้งรุ่น ไม่ว่าใครส่ง — เอาลิงก์เก่ามาส่งใหม่ก็ไม่ได้
- **ลิงก์ที่ไม่มี handle ของเจ้าตัว** จะขึ้นธงแดงให้โค้ชเห็นทันที
- **ส่งงานในสปรินต์ที่ไม่ได้ลงไม่ได้**
- **วันที่เท่าไหร่ ระบบคิดเอง** จากเวลาเซิร์ฟเวอร์ ไม่ได้เชื่อนาฬิกาในเครื่องนักเรียน
- **ตัดรอบตี 4** ไม่ใช่เที่ยงคืน เพราะคนทำคอนเทนต์โพสต์ดึก โพสต์ตีหนึ่งยังนับเป็นของเมื่อวาน
- **สปรินต์ที่เริ่มไปแล้วเปลี่ยนไม่ได้** ลงเพิ่มหรือถอนได้เฉพาะสปรินต์ที่ยังไม่เริ่ม
- **ลบงานไม่ได้ทั้งนักเรียนและโค้ช** งานที่ไม่ผ่านใช้วิธีตีกลับ เก็บประวัติไว้หมด
- **วันลา** สปรินต์ละ 2 วัน ขาดได้โดย streak ไม่ขาด (แก้จำนวนได้ใน `config.js`)

---

## ปรับให้เข้ากับหลักสูตรของคุณ

แก้ที่ `config.js` อย่างเดียว:

```js
SPRINT_DAYS: 14,        // สปรินต์ละกี่วัน
FREEZE_PER_SPRINT: 2,   // วันลาต่อสปรินต์
CUTOFF_HOUR: 4,         // ตัดรอบกี่โมง
SPRINTS: [ ... ]        // ชื่อกับโจทย์ของแต่ละสปรินต์ (จำนวนสปรินต์ = ความยาว array)
```

ถ้าเปลี่ยน `SPRINT_DAYS` หรือจำนวนสปรินต์ **ต้องแก้ในฐานข้อมูลด้วย**:

```sql
update public.cohort set sprint_days = 14, sprints = 6 where id = 1;
```

---

## คำสั่งที่ใช้บ่อยตอนดูแลรุ่น

ดูงานที่ค้างตรวจ:
```sql
select p.name, s.day_index, s.url, s.flag
from public.submissions s join public.profiles p on p.id = s.profile_id
where s.status = 'pending' order by s.created_at;
```

สรุปผลรายคน (เอาไปทำใบประกาศตอนจบรุ่น):
```sql
select p.name,
       count(distinct s.day_index) filter (where s.status='approved') as days,
       round(avg(s.stars) filter (where s.status='approved' and s.stars>0),2) as craft,
       count(*) as submitted
from public.profiles p left join public.submissions s on s.profile_id = p.id
group by p.name order by days desc;
```

ตั้งโค้ชเพิ่ม:
```sql
update public.profiles set role = 'coach' where name = 'ชื่อเขา';
```

---

## สิ่งที่ยังไม่มี — ต้องตัดสินใจก่อนทำเพิ่ม

- **การแจ้งเตือน** ระบบไม่เตือนใครเลย ถ้าอยากให้คนกลับมาทุกวัน ต้องมีตัวเตือนตอนสามทุ่ม
  ทางที่ได้ผลที่สุดคือบอทใน LINE กลุ่มรุ่น ซึ่งแก้ปัญหา "ลืมมาวางลิงก์" ไปพร้อมกันด้วย
- **ตรวจลิงก์อัตโนมัติ** ตอนนี้โค้ชเป็นคนดูว่าคลิปมีจริงไหม โพสต์วันนั้นจริงไหม
  ถ้ารุ่นใหญ่จนตรวจไม่ไหว ต้องต่อ API ของแพลตฟอร์มมาเช็คให้
- **แข่งเป็นทีม** ตอนนี้แข่งรายคน ถ้ารุ่นใหญ่กว่า 40 คน การแบ่งทีม 4–5 คนจะรักษาคนกลุ่มล่างได้ดีกว่ามาก
  แต่เปลี่ยนทีหลังแพง เพราะกระทบทั้งหน้าจอและฐานข้อมูล
- **หน้าจบรุ่น** ครบ 12 สัปดาห์แล้วยังไม่มีหน้าสรุปผล/ใบประกาศ
